extends: default
# ref. https://yamllint.readthedocs.io/en/stable/configuration.html
ignore-from-file:
  - .gitignore
rules:
  document-start: disable
  line-length:
    level: warning
    max: 200
  truthy: disable
  brackets:
    max-spaces-inside: 1
