@* The health-profile list row's second line (free-text notes). Extracted so the owner list page and the
   read-only shared-collection view render the identical meta from one definition. *@

@if (!string.IsNullOrEmpty(Item.Notes))
{
    <span>@Item.Notes</span>
}

@code {
    [Parameter] public required HealthProfileDto Item { get; set; }
}
