@* A monochrome inline "add" glyph (plus-in-circle), text-presentation like TrashIcon - no emoji codepoint.
   Wrap it in the action's own button (with a title tooltip); this is just the glyph. *@

<svg viewBox="0 0 24 24" width="@Size" height="@Size" fill="none" stroke="currentColor"
     stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
    <circle cx="12" cy="12" r="9"/>
    <path d="M12 8v8"/>
    <path d="M8 12h8"/>
</svg>

@code {
    [Parameter] public int Size { get; set; } = 17;
}
